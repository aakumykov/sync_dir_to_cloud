package com.github.aakumykov.sync_dir_to_cloud.a0_texts

APXUTEKTYPHblE_npoBJLEMbl

12.03.2025
Проблема: при добавлении в БД записи о новом элементе на основе старого
его дата модификации становится неактуальной и при следующей
проверке он помечается как изменившийся.
Возможные решения:
1) добавлять запись в БД, считывая инфу о файле из хранилища.
   Достоинство - полнота получаемой информации.
   Недостаток - медленная скорость, т.к. требует дополнительного
   запроса к хранилищу (а оно, чаще всего, будет облачным).
2) специальный флаг у SyncObject.

Подрешил, что нечего жалеть сетевые запросы в 21 веке.
Вариант с флагом чреват дополнительными проблемами.

